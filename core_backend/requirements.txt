# Django Core
Django~=4.2
gunicorn~=21.2

# Async & WebSockets (Django Channels)
channels~=4.0
daphne~=4.1
channels-redis~=4.2

# Database (PostgreSQL + pgvector)
psycopg2-binary~=2.9
pgvector~=0.3

# Authentication & Security
PyJWT~=2.8
cryptography~=42.0

# Async HTTP
aiohttp~=3.9


# Task Queues y Caching
celery>=5.3.6
redis>=5.0.0
django-redis~=5.4

# API Communication & Environment
# NOTE: microservice clients migrated to httpx (Sprint 4).
# requests retained for jwks.py, ai_models, and admin_views.
requests~=2.31
httpx~=0.26
django-environ~=0.11
python-dotenv~=1.0

# Third-party Django Apps
djangorestframework~=3.14
django-cors-headers~=4.3
django-storages~=1.14
whitenoise~=6.6

# Security & Hardening (M3/M5)
argon2-cffi>=23.1
django-axes>=6.1
tenacity>=8.2
bleach>=6.1

# PDF generation (we use WeasyPrint in ms3_reports; ReportLab removed)
# reportlab>=4.0

# Swagger
drf-spectacular~=0.27

