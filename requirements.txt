# --- DJANGO CORE ---
Django==5.2.7
djangorestframework==3.15.2
django-cors-headers==4.4.0
django-storages==1.14.6

# --- ASYNC & SERVER ---
# Uvicorn standard incluye uvloop (rapido) y websockets
uvicorn[standard]==0.30.0
gunicorn==22.0.0
# Daphne es vital para Channels en Windows
daphne==4.1.0
channels==4.1.0
channels-redis==4.2.0

# --- DATABASE & VECTORS ---
# Usamos la version binary para evitar compilar en Windows
psycopg[binary]>=3.1.18
redis==5.0.4
# ESTE ES EL QUE TE FALTABA para el error de pgvector
pgvector>=0.2.5

# --- UTILS ---
python-dotenv==1.0.1
requests>=2.31.0
httpx>=0.27.0
# Necesario para manejo de imagenes si subes avatares/logos
Pillow>=10.3.0
whitenoise